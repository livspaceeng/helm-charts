{{- if .Values.config.enabled -}}
{{ $config :=  .Values.config.convoy | toPrettyJson  }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "convoy.fullname" . }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
data:
  convoy.json: {{ $config }}
{{ end -}}
